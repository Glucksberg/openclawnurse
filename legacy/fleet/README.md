# Legacy Fleet

This directory preserves the old fleet/multihost experiment for reference.

It is not part of the active OpenClawNurse runtime:

- the installer does not copy these files
- no fleet timers or cron entries are installed
- the main smoke test does not exercise this code
- the README does not describe fleet as a supported program feature

Use this only as archived design and implementation context.
