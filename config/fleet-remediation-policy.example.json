{
  "mode": "advisory",
  "allowedActions": [
    "run_nurse_now",
    "restart_gateway",
    "replay_notification"
  ],
  "manualApprovalRequiredActions": [
    "restart_gateway"
  ],
  "forbiddenPatterns": [
    "rm -rf",
    "git reset --hard",
    "pm2 delete",
    "systemctl disable",
    "kill -9"
  ],
  "maxAutoActionsPerRun": 2,
  "maxNodesPerRun": 3,
  "notes": "Default is advisory. To allow openclaw-fleet-remediation-exec.sh --execute to run eligible local actions, set mode to execute and keep allowed actions, manual approvals and limits explicit."
}
