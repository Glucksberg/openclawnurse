{
  "mode": "advisory",
  "allowedActions": [
    "run_nurse_now",
    "restart_gateway",
    "replay_notification"
  ],
  "manualApprovalRequiredActions": [
    "restart_gateway"
  ],
  "forbiddenPatterns": [
    "rm -rf",
    "git reset --hard",
    "pm2 delete",
    "systemctl disable",
    "kill -9"
  ],
  "maxAutoActionsPerRun": 2,
  "maxNodesPerRun": 3,
  "notes": "The LLM may classify incidents and summarize evidence, but execution must stay inside the allowed action list and forbidden patterns."
}
